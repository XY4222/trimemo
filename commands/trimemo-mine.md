---
description: Mine projects and conversations into the TriMemo. Supports project files, conversation exports, and auto-classification.
---

Invoke the `trimemo` skill from this plugin and run the `mine` instructions, then follow them.

Concretely: run `trimemo instructions mine` in a terminal, then carry out the steps it prints.
