---
description: Show the current state of your memory palace — wings, rooms, drawer counts, and suggestions.
---

Invoke the `trimemo` skill from this plugin and run the `status` instructions, then follow them.

Concretely: run `trimemo instructions status` in a terminal, then carry out the steps it prints.
