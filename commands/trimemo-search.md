---
description: Search your memories across the TriMemo using semantic search with wing/room filtering.
---

Invoke the `trimemo` skill from this plugin and run the `search` instructions, then follow them.

Concretely: run `trimemo instructions search` in a terminal, then carry out the steps it prints. The MCP tool `mempalace_search` is also available directly from this Cursor session.
